# ═══════════════════════════════════════════════════════════════
# Reusable Docker Build / Scan / Push / Sign — ADZA-Group (senior)
#
# build(load) → size-gate → Trivy image scan (dual-gate) → smoke → SBOM
# and when push=true: GHCR login → metadata tags → :previous backup (main)
# → build+push (multi-arch) → cosign keyless sign + SLSA provenance.
#
# Usage:
#   uses: adza-group/shared-workflows/.github/workflows/reusable-docker-build.yml@v1
#   with:
#     image-name: ghcr.io/adza-group/my-app
#     push: true
#     sign-image: true
#     platforms: "linux/amd64,linux/arm64"
#   secrets: inherit
# ═══════════════════════════════════════════════════════════════

name: "🐳 Docker Build"

on:
  workflow_call:
    inputs:
      image-name:
        required: true
        type: string
        description: "Full image name (e.g. ghcr.io/adza-group/my-app)"
      dockerfile:
        required: false
        type: string
        default: "Dockerfile"
      context:
        required: false
        type: string
        default: "."
      runner-label:
        required: false
        type: string
        default: '["self-hosted", "linux", "proxmox"]'
      push:
        required: false
        type: boolean
        default: false
      platforms:
        required: false
        type: string
        default: "linux/amd64"
      image-size-limit-mb:
        required: false
        type: number
        default: 800
      run-trivy:
        required: false
        type: boolean
        default: true
      generate-sbom:
        required: false
        type: boolean
        default: true
      sign-image:
        required: false
        type: boolean
        default: true
      gated-promotion:
        required: false
        type: boolean
        default: false
        description: "main pushes :candidate-<sha> instead of :latest; a later promote job retags after staging is green"
      build-args:
        required: false
        type: string
        default: ""
      cache-from:
        required: false
        type: string
        default: "type=gha"
      smoke-command:
        required: false
        type: string
        description: "Optional shell command to smoke-test the loaded image ($IMAGE env = ci tag)"
        default: ""
      tag-config:
        required: false
        type: string
        description: "docker/metadata-action tags block (deploy tags :staging/:latest werden ref-basiert vom Workflow ergänzt — NICHT hier definieren)"
        default: |
          type=ref,event=branch
          type=ref,event=tag
          type=sha,prefix={{branch}}-,format=short
    outputs:
      image-tag:
        description: "Local CI image tag (single-arch, loaded)"
        value: ${{ jobs.build.outputs.tag }}
      image-size-mb:
        description: "Image size in MB"
        value: ${{ jobs.build.outputs.size }}
      digest:
        description: "Pushed image digest (empty when push=false)"
        value: ${{ jobs.build.outputs.digest }}
      candidate-tag:
        description: "Candidate tag pushed when gated-promotion on main (empty otherwise)"
        value: ${{ jobs.build.outputs.candidate }}

jobs:
  build:
    name: "🐳 Build + Scan + Push/Sign"
    runs-on: ${{ fromJSON(inputs.runner-label) }}
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    outputs:
      tag: ${{ steps.localtag.outputs.tag }}
      size: ${{ steps.size.outputs.mb }}
      digest: ${{ steps.build-push.outputs.digest }}
      candidate: ${{ steps.candidate.outputs.tag }}
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3

      - name: Set up QEMU (multi-arch only)
        if: ${{ inputs.push && inputs.platforms != 'linux/amd64' }}
        uses: docker/setup-qemu-action@06116385d9baf250c9f4dcb4858b16962ea869c3  # v4.1.0

      - uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5  # v4.1.0

      - name: Build (single-arch, load for scan)
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf  # v7.2.0
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          push: false
          load: true
          tags: ${{ inputs.image-name }}:ci-${{ github.run_id }}
          cache-from: ${{ inputs.cache-from }}
          cache-to: type=gha,mode=max
          build-args: |
            ${{ inputs.build-args }}
            GIT_SHA=${{ github.sha }}

      - name: Local tag output
        id: localtag
        run: echo "tag=${{ inputs.image-name }}:ci-${{ github.run_id }}" >> "$GITHUB_OUTPUT"

      - name: "📏 Image Size Gate"
        id: size
        run: |
          REF="${{ inputs.image-name }}:ci-${{ github.run_id }}"
          SIZE_BYTES=$(docker image inspect "$REF" --format='{{.Size}}')
          MB=$((SIZE_BYTES / 1024 / 1024))
          LAYERS=$(docker image inspect "$REF" --format='{{len .RootFS.Layers}}')
          echo "mb=$MB" >> "$GITHUB_OUTPUT"
          {
            echo "## 🐳 Docker Image"
            echo "| Metric | Value |"
            echo "|--------|-------|"
            echo "| Size | **${MB} MB** |"
            echo "| Layers | ${LAYERS} |"
            echo "| Limit | ${{ inputs.image-size-limit-mb }} MB |"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "$MB" -gt "${{ inputs.image-size-limit-mb }}" ]; then
            echo "::error::Image too large: ${MB} MB (max ${{ inputs.image-size-limit-mb }} MB)"; exit 1
          fi

      - name: "🔍 Trivy Image Scan (gate)"
        if: ${{ inputs.run-trivy }}
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: ${{ inputs.image-name }}:ci-${{ github.run_id }}
          format: table
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: ${{ (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')) && '1' || '0' }}

      - name: "🔥 Smoke boot"
        if: ${{ inputs.smoke-command != '' }}
        env:
          IMAGE: ${{ inputs.image-name }}:ci-${{ github.run_id }}
        run: ${{ inputs.smoke-command }}

      - name: "📋 SBOM (CycloneDX)"
        if: ${{ inputs.generate-sbom }}
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: ${{ inputs.image-name }}:ci-${{ github.run_id }}
          format: cyclonedx
          output: sbom.json

      - name: Upload SBOM
        if: ${{ inputs.generate-sbom }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom
          path: sbom.json
          retention-days: 90

      - name: GHCR login
        if: ${{ inputs.push }}
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee  # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata (tags + labels)
        if: ${{ inputs.push }}
        id: meta
        uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9  # v6.1.0
        with:
          images: ${{ inputs.image-name }}
          # Deploy-Tags ref-basiert (NICHT {{is_default_branch}}: ADZA-Repos haben dev als
          # Default-Branch → is_default_branch taggte :latest auf dev-Builds → Watchtower
          # deployte dev-Images auf PROD; :staging wurde dagegen nie getaggt → Staging fror ein).
          tags: |
            ${{ inputs.tag-config }}
            type=raw,value=staging,enable=${{ github.ref == 'refs/heads/dev' }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' && !inputs.gated-promotion }}
            type=raw,value=candidate-${{ github.sha }},enable=${{ github.ref == 'refs/heads/main' && inputs.gated-promotion }}

      - name: "Backup :latest -> :previous (main only)"
        if: ${{ inputs.push && github.ref == 'refs/heads/main' && !inputs.gated-promotion }}
        continue-on-error: true
        run: |
          if docker buildx imagetools inspect ${{ inputs.image-name }}:latest >/dev/null 2>&1; then
            docker buildx imagetools create --tag ${{ inputs.image-name }}:previous ${{ inputs.image-name }}:latest
            echo "::notice::backed up :latest -> :previous"
          else
            echo "::notice::no existing :latest to back up (first push)"
          fi

      - name: "Backup :staging -> :staging-previous (dev only)"
        if: ${{ inputs.push && github.ref == 'refs/heads/dev' }}
        continue-on-error: true
        run: |
          if docker buildx imagetools inspect ${{ inputs.image-name }}:staging >/dev/null 2>&1; then
            docker buildx imagetools create --tag ${{ inputs.image-name }}:staging-previous ${{ inputs.image-name }}:staging
            echo "::notice::backed up :staging -> :staging-previous"
          else
            echo "::notice::no existing :staging to back up (first push)"
          fi

      - name: Build + push (multi-arch)
        if: ${{ inputs.push }}
        id: build-push
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf  # v7.2.0
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          push: true
          platforms: ${{ inputs.platforms }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: ${{ inputs.cache-from }}
          cache-to: type=gha,mode=max
          build-args: |
            ${{ inputs.build-args }}
            GIT_SHA=${{ github.sha }}

      - name: Candidate tag output
        if: ${{ inputs.push && inputs.gated-promotion && github.ref == 'refs/heads/main' }}
        id: candidate
        run: echo "tag=${{ inputs.image-name }}:candidate-${{ github.sha }}" >> "$GITHUB_OUTPUT"

      - name: Install cosign
        if: ${{ inputs.push && inputs.sign-image }}
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2

      - name: Cosign sign (keyless)
        if: ${{ inputs.push && inputs.sign-image }}
        continue-on-error: true   # best-effort: Sigstore/Rekor outages or org limits must not fail a pushed build
        env:
          DIGEST: ${{ steps.build-push.outputs.digest }}
        run: cosign sign --yes "${{ inputs.image-name }}@${DIGEST}"

      - name: SLSA build provenance
        if: ${{ inputs.push && inputs.sign-image }}
        continue-on-error: true   # GitHub Artifact Attestations need a paid plan / public repo; advisory on free private orgs
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32  # v4.1.0
        with:
          subject-name: ${{ inputs.image-name }}
          subject-digest: ${{ steps.build-push.outputs.digest }}
          push-to-registry: true
